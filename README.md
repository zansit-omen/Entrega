Para crear cualquier archivo y ejecutarlo exitosamente debemos crear un entorno virtual bajo cualquier nombre deseado y posteriormente activarlo
Para ejecutar la aplicación correspondiente a flask debemos descargar la biblioteca flask con la linea de codigo pip install flask. Despues debemos buscar flask en google, copiar el codigo mostrado y pegarlo en un nuevo archivo llamado app.py. 
Posteriormente, ejecutaremos el codigo de app.py y entraremos a la IP devuelta por la terminal, a la cual le agregaremos al final /inicio. 

Para ejecutar el archivo EjemploClase1 simplemente usaremos la linea de codigo: python EjemploClase1.py en la terminal.
